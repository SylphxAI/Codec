/**
 * Adobe Photoshop (PSD) codec
 *
 * Features:
 * - Decode flattened composite image
 * - 8-bit and 16-bit depth
 * - RGB, Grayscale, CMYK color modes
 * - RLE compression support
 * - Parse layer info (names, bounds, opacity)
 */

export * from './types'
export * from './decoder'
