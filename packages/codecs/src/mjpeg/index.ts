/**
 * MJPEG (Motion JPEG) codec
 *
 * Features:
 * - Decode raw MJPEG streams (concatenated JPEGs)
 * - Extract individual frames
 * - Encode image sequences to MJPEG
 * - Frame-by-frame access
 */

export * from './types'
export * from './decoder'
export * from './encoder'
