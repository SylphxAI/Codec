/**
 * XPM (X Pixmap) format types and constants
 * Text-based image format used in X Window System
 */

export interface XPMEncodeOptions {
	charsPerPixel?: number // 1 or 2 (default: auto based on color count)
}
