/**
 * @mconv/wasm - WebAssembly acceleration layer
 *
 * Provides optional WASM backends for CPU-intensive operations.
 * Falls back to pure TypeScript when WASM is unavailable.
 */

export * from './loader'
